# lojack_clients

Lightweight LoJack client library implemented with `aiohttp` to avoid `httpx` dependency conflicts with Home Assistant.

Quick start

1. Install: `pip install .[dev]` (from repo root)
2. Use `lojack_clients.LoJackClient` to authenticate and list devices.
